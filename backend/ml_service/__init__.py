"""JobFlow-GNN ML Service — importable library for GNN-based CV-Job matching.

Core modules:
    inference   — InferenceEngine, MatchResult, JobMatchResult
    crawler     — CrawlProvider, RawJob, SkillExtractor
    cv_parser   — CVParser
    embedding   — EmbeddingProvider
    graph       — GraphBuilder, schema
    models      — HeteroGraphSAGE, HeteroRGCN
    training    — Trainer
    reranker    — Reranker, FeatureExtractor
    evaluation  — metrics
"""
